{
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "dev": "json-server --watch data/db.json --port 3001",
    "start": "json-server --watch data/db.json --port 3001"
  }
}
